class Interact {
    static AddPeekEntryByModel(pModels: number[], pData, pOptions) {
        globalThis.exports['np-interact'].AddPeekEntryByModel(pModels, pData, pOptions)
    }

    static AddPeekEntryByPolyTarget(pEvent: string, pData, pOptions) {
        globalThis.exports['np-interact'].AddPeekEntryByPolyTarget(pEvent, pData, pOptions)
    }

    static AddPeekEntryByEntityType(pEntity, pData, pOptions) {
        globalThis.exports['np-interact'].AddPeekEntryByEntityType(pEntity, pData, pOptions)
    }
}